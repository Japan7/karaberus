{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "specific permissions for desktop platforms",
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["updater:default", "shell:default"]
}
